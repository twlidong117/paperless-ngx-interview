# 学习进度

更新日期：2026-09-10。由 Codex 辅助整理。

## 当前阶段状态

**阶段完成，暂时冻结。** 本次是阶段收尾，不新增学习步骤。职业能力学习主线调整为 Java 21（编程语言）与 Spring Boot（用于构建 Java 应用的开发框架）；Paperless-ngx 保留为 Python/Django 大型项目阅读与跨技术栈比较样本。技术栈指一个项目采用的一组语言、框架和配套工具。

暂停不代表已经掌握 Django、Paperless-ngx 或完整 Python 后端开发。第七步整体未通过的问题保留，不因冻结而改成通过。完整说明见 [阶段总结](PHASE_SUMMARY.md)。

本次收尾读取 GitHub 的 dev 分支，起点提交标识为 `e2a1657c999ed0e99f4725696e0b29ea1a608d77`。提交标识用于定位一次代码历史记录。本次仅核对仓库文档和合并记录，没有重新进入原运行环境验收。下方历史记录中的“本次”指对应检查轮次。

## 项目与证据来源

- 自己的仓库：https://github.com/twlidong117/paperless-ngx-interview
- 原项目：https://github.com/paperless-ngx/paperless-ngx
- GitHub 默认分支：dev，即开发分支；这不是对每次任务实际分支的断言。
- 用户提供的 Codex 检查报告：工作目录 /workspace/paperless-ngx-interview；实际分支 work；当前提交 c4fe23eceef9b8c1a744778885251194c195f60e；父提交 ec70e4f42324ba4706b5a2b582cbb87bf45b1fe3；远程仓库列表为空；工作目录状态检查没有输出。
- 助手独立核对：在本次文档更新前，GitHub 开发分支的提交为 c4fe23eceef9b8c1a744778885251194c195f60e，与报告一致。这确认代码历史起点对应，不证明任务环境具备 GitHub 写入权限。
- 证据边界：助手没有进入用户那次 Codex 环境重新执行命令。环境结果来自用户粘贴的报告；GitHub 结果来自当前聊天的独立连接。
- 本次记录更新会产生新的提交；上面的提交标识是检查时的历史状态，不是对未来最新状态的保证。
- 运行环境的软件与前置条件已在第五步通过只读命令核验；第七步已取得部分项目依赖可加载的证据，完整依赖验收未通过，应用启动和功能验证仍未执行。
- 教学约定：先读 AGENTS.md，再读本文件及最近讲义。
- 第四步资料依据：当前副本的 README.md、docs/index.md 和 docs/usage.md；这是资料阅读，不是应用运行证据。
- 第五步检查时的实际代码分支为 work，检查起点提交为 abd0f344f4bda96fe91dd696a5ba99d43a73a4af；第五步讲义提交标识为 c985208457f35e8575925b2cbc161c0f612cb499。
- 第五步资料依据：当前副本的 docs/development.md、docs/setup.md、pyproject.toml、src-ui/package.json 和 docker/compose/docker-compose.sqlite.yml，以及只读系统检查结果。
- 第六步首次执行时的实际代码分支为 work，起点提交为 6a9d23cbcfde1da5f21ef5b2cb05e3e5a6f6812e；当时新增 learning/steps/06-uv-version.md，记录 uv 0.7.22 与安装受阻结果。
- 2026-09-10 在 work 分支、起点提交 4b36f6d6b76c885424c99cc405b38779eb5f744a 上重新只读验收：入口 `/root/.local/bin/uv` 解析到 `/root/.local/share/pipx/venvs/uv/bin/uv`，版本为 0.12.12，满足项目最低要求 0.9.0。
- 第七步开始时实际分支为 work，起点提交为 21de5e1750c4e2c10f6243ec06c9d1175d60163c；本步只验收准备脚本安装后的 Python 环境，不启动应用。

## 完成状态

| 步骤 | 状态 | 证据 |
| --- | --- | --- |
| 01 创建并核验派生仓库 | 已完成 | 仓库来源和归属已核验 |
| 02 保存教学规则和进度 | 已完成 | 教学提交 c4fe23eceef9b8c1a744778885251194c195f60e 已发布，四份文件曾逐一读回核对 |
| 03 核验 Codex 文件读取与教学理解 | 已通过，已记录 | 用户检查报告列出实际文件、规则、进度、分支、提交；提交与 GitHub 交叉核对一致 |
| 04 理解项目解决的问题与主要使用流程 | 已完成，已核验 GitHub 发布 | 已读取 README.md、docs/index.md、docs/usage.md，并新增 learning/steps/04-business-workflow.md；当前聊天已从 GitHub 开发分支读取讲义与本进度，核对主要资料依据；未运行应用 |
| 05 检查运行 Paperless-ngx 的前置条件 | 已完成，复核补正后已确认 GitHub 发布 | 已读取开发与安装说明并执行只读环境检查，新增 learning/steps/05-runtime-prerequisites.md，讲义提交为 c985208457f35e8575925b2cbc161c0f612cb499；未安装或启动应用 |
| 06 核验 uv 的执行位置和版本 | 已完成 | 2026-09-10 只读复核得到入口 `/root/.local/bin/uv`、实际文件 `/root/.local/share/pipx/venvs/uv/bin/uv` 和版本 0.12.12；程序化比较确认满足 `>=0.9.0`，五项退出状态均为 0 |
| 07 验收准备脚本安装的 Python 依赖 | 已执行，整体未通过 | `.venv/bin/python` 成功从项目虚拟环境加载 Django 5.2.16 和 Celery 5.6.3；`uv sync --check` 获取 `psycopg-c` 构建产物时因网络隧道错误返回 2；`pyproject.toml` 与 `uv.lock` 前后哈希一致且 Git 无差异；未启动应用 |
| 项目依赖准备 | 部分结果已验证，完整验收未通过 | 第七步已从项目虚拟环境加载 Django 和 Celery；完整检查被网络错误阻断，不能沿用第五步“安装未开始”的状态 |
| 应用启动、数据库迁移与功能验证 | 未执行 | 没有应用运行或完整业务流程验证证据；数据库迁移指按程序要求建立或更新数据表结构 |
| 学习文档发布 | GitHub 发布已核验；原任务环境直接推送能力未重新核验 | 第四号拉取请求已合并，第七步讲义也已存在于本次读取的 dev 分支；拉取请求是请他人审阅并合并一组修改的记录。不能用文档发布事实推断原本地副本的远程配置或权限 |

## 冻结期间与恢复入口

当前没有待执行的新学习步骤。不安装、不升级、不启动应用，也不自动重试第七步。历史讲义中的下一步预告仅保留为历史记录。

以后用户明确要求恢复时，第一步先读取 AGENTS.md、本文件、[阶段总结](PHASE_SUMMARY.md) 与 [第七步讲义](steps/07-python-dependency-check.md)，核对届时实际分支、提交和环境，再确定一个小目标。若选择恢复运行验证，先复核网络访问条件与原失败点；若只做跨技术栈阅读比较，不必先启动应用。本次不执行上述动作。

## 历史检查记录与未解决问题

以下保留各次检查当时的事实，不作为冻结期间的执行清单。尤其第五步的软件缺失和目录不存在，不能覆盖第六、七步的较新结果。

- work 是报告中的任务分支名，dev 是 GitHub 默认分支名。检查时二者指向同一提交，不因名称不同而要求切换。
- 远程列表为空表示当前副本未配置远程仓库，不等于没有读取权限或写入权限；没有证据确定为空的具体原因。当前可继续读取学习，真正发布代码前再核验支持的发布方式。
- 工作目录干净表示 Git 在该命令和配置下没有报告未提交变化，不表示业务功能正确，也不表示编辑器中没有未保存内容。
- 应用运行所需软件与代码起点选择仍待后续运行步骤核实。
- 当前没有个人功能改进或运行测试成果，面试时如实描述。
- 第四步只完成业务用途与主要流程的资料学习；应用页面、导入、光学字符识别、自动匹配和搜索结果均未实际运行验证。

- 第四步复核：讲义和进度已能从 GitHub 开发分支读取，发布事实已确认；具体发布途径与用户 Codex 环境的写入权限不据此推断。已澄清全局搜索与文档搜索的区别，并注明业务讲解编号不代表程序内部严格执行顺序。
- 第五步确认环境是 Ubuntu 24.04.4 LTS、x86_64；Python 3.14.4、uv 0.7.22、Node.js 24.15.0 和 SQLite JSON1 可用。
- 第五步确认 Docker、Docker Compose、本地消息代理、关键 Python 项目依赖与主要光学字符识别和文档转换工具缺失；pnpm 10.28.1 可执行但与项目声明的 11.15.1 不一致。
- 第五步检查时 paperless.conf、.venv、consume 和 media 尚不存在；src-ui/node_modules 存在只证明目录存在，不证明前端构建成功。
- 第五步没有安装、启动、迁移或功能测试，因此应用运行状态仍是未验证。
- 外部官方页面读取受到 401 和 403 响应限制；第五步依据当前仓库中的官方文档源文件整理，没有虚构网页访问成功。

- 2026-09-09 第五步复核：GitHub 开发分支已可读取第五步讲义和进度，发布事实已确认；当时的“当前环境发布能力未验证”仅表示未确认原任务环境直接发布能力，不表示这些文件未发布。
- 项目明确要求 uv >=0.9.0，第五步记录的 uv 0.7.22 不满足，已补正讲义。实际执行第六步时重新检查环境，不能假设历史版本仍是当前版本。
- libmagic 是共享库，不能凭命令不存在断定库缺失；后续安装前按需要核验实际库加载能力。
- 2026-09-09 第六步重新检查：`/root/.local/bin/uv` 解析到 `/root/.local/share/pipx/venvs/uv/bin/uv`，实际仍为 0.7.22；pipx 1.4.3 元数据记录原安装规格 `uv==0.7.*`。
- 第六步选用与现有 pipx 安装方式一致的 `pipx install --force 'uv>=0.9.0'`，但 Python Package Index 查询和 GitHub 官方发布页均被环境代理以 403 拒绝；本地没有其他 uv 或缓存安装包。旧环境未被删除并已复核仍可用。
- 2026-09-09 的失败轮次没有安装任何项目依赖、没有修改 `pyproject.toml` 的版本要求、没有启动应用；当时因此不能把工具准备表述为已完成。该历史判断已由下一条 2026-09-10 的重新验收结果更新。
- 2026-09-10 第六步复核：`type -a uv` 只列出 `/root/.local/bin/uv`，其解析后的文件为 `/root/.local/share/pipx/venvs/uv/bin/uv`；`uv --version` 返回 `uv 0.12.12 (x86_64-unknown-linux-gnu)`，版本比较通过。
- 本次没有执行安装或升级，不能把从昨日 0.7.22 到今日 0.12.12 的变化归因于本次操作，也不推断更新主体或方式。没有安装其他依赖、没有启动应用。
- 2026-09-10 外部官方文档仍未能读取：网页检索接口返回 `401 Unauthorized`，`curl` 读取 uv 与 pipx 官方页面均返回 HTTP 403。讲义保留官方链接并准确记录限制，没有据此虚构页面内容。
- 2026-09-10 第七步验收：`.venv/bin/python` 的 `sys.executable` 为项目虚拟环境入口；Django 5.2.16 与 Celery 5.6.3 均从 `.venv/lib/python3.12/site-packages` 成功导入。
- 第七步的 `uv sync --check` 返回 2；第一个有意义的错误是访问项目指定的 `psycopg-c` GitHub 构建产物时网络隧道连接失败。该结果不能证明完整锁定环境一致，也不推翻两个框架已成功导入的事实。
- 第七步检查前后 `pyproject.toml` 与 `uv.lock` 的对象哈希完全一致，指定文件的 Git 状态无输出且差异检查返回 0；没有降级或删除依赖，没有启动应用。
- 第七步官方文档核验受限：网页检索返回 401，四个官方页面的 `curl` 请求均返回 HTTP 403；讲义准确记录限制，不声称已读取页面正文。
